arquivo de configuração para a rotação de logs
	--> rotação de logs refere a uma gerência inteligente de logs utilizando o cron, fazendo a compactação dos logs todo mês (monthly), semana (weekly), dia (daily) ou hora (hourly)

logrotate -f /path/to/logrotate.conf
	--> força a execução do logrotate
